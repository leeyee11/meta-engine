import { ActionBase } from '@meta-engine/action';
import { Context } from '@meta-engine/sandbox';
declare const _default: (context: Context, action: ActionBase) => Promise<Context>;
export default _default;
